
app:
  auth:
    shared-secret: SECRET

spring:
  security:
    user:
      name: test
      password: test
  datasource:
    url: jdbc:h2:mem:poupei
    username: poupei
    password: poupei@01
    driverClassName: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true
    show-sql: true